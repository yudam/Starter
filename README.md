# Starter

Android任务启动器,

```
 TaskDispatchers.init()
            .debuggable(true)
            .addTask(Init1Task())
            .addTask(Init2Task())
            .addTask(Init3Task())
            .addTask(Init4Task())
            .addTask(Init5Task())
            .start()
```